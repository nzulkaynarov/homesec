# CLAUDE.md — контекст для ИИ-агентов

HomeSec — работающая система контроля домашней сети. Это НЕ учебный проект:
изменения в `main` автоматически деплоятся в реальную домашнюю сеть семьи.
Ошибка в правилах DNS/firewall оставляет дом без интернета.

## Текущее состояние (2026-07-28): РАЗВЁРНУТО И РАБОТАЕТ, но см. ЧП ниже

Срез для продолжения работы (обновлять при передаче между машинами):

- **Полный аудит кода и прода — `docs/09-audit-2026-07.md`** (102 находки:
  1 critical, 16 high, 38 medium, 47 low; ✅ = лично подтверждено по коду).
  Начинать с блока «Рекомендованный порядок работ» в шапке того файла.
- **Первая волна исправлений по аудиту — ветка `audit/2026-07` (PR, ждёт
  merge владельцем).** Закрыто: CRITICAL (аварийный откат DNS), живучесть
  деплоя, меры после ЧП, HIGH-баги ядра (квота/удаление/merge/инъекции),
  UX детских страниц, тесты на mikrotik.py и fail-режимы reconcile.
  pytest 129 / ruff / mypy зелёные. Подробности — в сообщениях коммитов.
  **Требует ручных шагов на железе** (агент этого сделать не мог, см. ниже
  «Что осталось руками»).
  Не закрыто из аудита (следующие кандидаты): квота на человека, а не на
  устройство (2 гаджета = 2× лимит); карантин для новых устройств со
  случайным MAC; роли/второй родитель; IP как идентификатор на детских
  страницах (лечится на роутере: `add-arp=yes` + `arp=reply-only`);
  идемпотентность кнопок `pick:`/`nd:` в боте.

- Бот и ИИ запущены на проде и работают. Инцидент «AdGuard 400 каждую
  минуту» ЗАКРЫТ — корней было три слоями: креды (401) → дубли IP от
  ротации приватных MAC → невалидные id сервисов «ea»/«kick» в коде
  (истинный корень; id теперь сверяются с реестром живого AdGuard в
  sync_clients, неизвестные отбрасываются с warning).
- **PR #2 (bot-sprint1) СМЕРЖЕН** (спринт 1 аудита бота в проде): меню
  команд, живучие кнопки подтверждений (таблица pending_actions,
  миграция №2), выбор устройств кнопками, /devices v2, уведомление о
  квотной блокировке с «+30 мин», экранное время в /status и дайджесте.
  Локальные ветки/worktree'ы агентов прибраны (Mac, 2026-07-28); на
  GitHub остались смерженные origin/bot-sprint1 и origin/claude/… —
  можно удалить.
- Продуктовый аудит бота: docs/08-bot-product-audit.md (спринт 1 — в
  PR #2; спринты 2–3 — на согласование: запрос «+время» со страницы
  /blocked, пресеты пауз, /merge, один писатель в RouterOS, роли).
- Открыто у владельца: merge iPhone-дублей кнопкой бота;
  решение по HS_AI_DAILY_TOKEN_BUDGET (300k сгорают за вечер отчётов);
  сцепка «CD деплоит только зелёный CI» (предложено, не решено);
  спринты 2–3 аудита бота (docs/08).

### ЧП 2026-07-27: малинка зависла — ПРИЧИНА НЕ НАЙДЕНА, план готов

Pi самопроизвольно завис (подозрение: OOM на 1 ГБ RAM или переполнение
диска логами), владелец обесточивал всю группу. Пока Pi лежит,
принудительный DNS уводит весь дом в никуда (правило №1) — поэтому
план ниже. НА МОМЕНТ ЗАПИСИ ДИАГНОСТИКА НЕ ПРОВОДИЛАСЬ (малинка была
недоступна с Mac); продолжать с машины с доступом к 192.168.88.2.

**Шаг 1 — вскрытие. Одной командой на малинке (собирает всё сразу: следы
OOM в прошлой загрузке, место на SD, топ по памяти, состояние watchdog):**

```bash
sudo bash /opt/homesec/deploy/pi-hardening.sh --diagnose
```

ДИАГНОСТИКА ТАК И НЕ ПРОВОДИЛАСЬ: 2026-07-28 сеть дома была доступна, но
ssh на малинку заблокирован политикой разрешений агента — выполнить руками.

**Шаг 2 — меры (владелец одобрил «watchdog вместо ежедневного ребута»;
cron-reboot отвергнут как костыль, маскирующий причину). Реализованы в
ветке `audit/2026-07`, применяются двумя ручными действиями:**

```bash
sudo bash /opt/homesec/deploy/pi-hardening.sh   # watchdog + журналы + zram + retention AdGuard
```

- watchdog (`RuntimeWatchdogSec=15`), потолок journald (64 МБ), zram-swap,
  обрезка журнала AdGuard (24 ч) и статистики (7 дней) — всё в этом скрипте,
  он идемпотентный; если `/dev/watchdog` нет, добавит `dtparam=watchdog=on`
  и попросит перезагрузку;
- `MemoryHigh/MemoryMax` для homesec и homesec-bot приезжают обычным
  деплоем (update.sh теперь переустанавливает юниты при изменении).

На роутере (залить `mikrotik/pi-guard.rsc` в Files, вписать токен бота и
chat_id, затем `/import file-name=pi-guard.rsc`) — сторож малинки: раз в
минуту пинг, три неудачи подряд → роутер сам берёт DNS на себя и шлёт алерт
в Telegram, малинка вернулась → контроль возвращается сам. DHCP не трогает:
вместо этого включает заранее заведённые ВЫКЛЮЧЕННЫЕ правила «спасательного
круга» (заворот DNS-запросов к 88.2 на роутер + hairpin по правилу №3),
поэтому лечится мгновенно и для всех, включая гостей.
**Проверить вживую обязательно** (выключить малинку и посмотреть
`/log print where message~"hs:"`) — на роутере правила не тестировались.

Аварийный откат руками (тоже новое, залить `emergency-dns-rollback.rsc`):
`/import file-name=emergency-dns-rollback.rsc` — см. docs/05-operations.md.

### Что осталось руками (агент не мог: нет доступа к железу)

1. Диагностика ЧП: `pi-hardening.sh --diagnose` на малинке (причина
   зависания до сих пор неизвестна).
2. `pi-hardening.sh` (без флага) — применить меры.
3. Залить на роутер `emergency-dns-rollback.rsc` и `pi-guard.rsc`
   (во втором — вписать токен бота и chat_id), импортировать, проверить.
4. Смержить PR ветки `audit/2026-07` — до merge всё это в проде НЕ работает.

### Аудит и правки 2026-07-15 (все в проде, main)

- **DHCP-DNS фикс**: LAN-сеть роутера раздавала DNS 88.1 вместо 88.2 →
  AdGuard видел только роутер как клиента, персональные политики (safe
  search детям и т.п.) не работали. Исправлено (`/ip dhcp-server network`
  через `[find comment=defconf]`; селектор `find address=` НЕ матчит
  IP-префикс на 7.23!). Теперь AdGuard видит реальные IP устройств.
- **Анти-обход восстановлен**: DoH-блок был на все порты (ломал upstream) →
  заменён на drop tcp+udp/443 к hs-doh для hs-managed; DoT(853) включён.
  QUIC-для-детей осознанно ВЫКЛЮЧЕН (не рвать стриминг на TV).
- **Роутер**: отключены telnet/ftp/api-ssl/reverse-proxy (открыты только
  ssh/www/winbox с LAN и api для Pi).
- **Контроль детей**: глобальный NSFW-блоклист HaGeZi в AdGuard (панель его
  не трогает); включена квота игр детям 120 мин/день.
- **Хардненинг ядра** (commit harden…): `get_self_ips` fail-safe —
  статический якорь `HS_SELF_IPS` (по умолч. 192.168.88.2) + fail-closed в
  reconcile (пустой self_ip → роутер не трогаем); межпроцессный flock вокруг
  reconcile (панель и бот — разные процессы); IntegrityError в
  discover_devices; троттлинг журнала ошибок (30 мин).
- **Бот**: опц. `HS_TELEGRAM_USER_IDS` — доп. проверка from_user.id (защита
  групповых чатов). Пусто = прежнее поведение.
- **Панель**: дашборд → родительская консоль (карточки по людям, полосы
  экранного времени, онлайн, пауза); быстрые действия в один тап (пауза
  30м/1ч/до утра, +30 мин бонус, групповая пауза/«Ужин»); security-заголовки
  (CSP, X-Frame-Options), rate-limit логина (8 попыток/15 мин → 429).
### Киллер-фича «попросить ещё времени» (2026-07-16, в проде, main)

- Реализована (аналог Family Link под сетевой контроль без агента на
  устройстве). Ключевой разворот: страница-перехват на HTTP ребёнку почти
  не видна в мире HTTPS, поэтому канал к нему — САМА панель на LAN
  (`/me`, публичная): открывает наш хост напрямую → нет проблемы
  сертификата, и она доступна даже при полной блокировке (hs-blocked →
  панель:8000 разрешён). Captive-portal для полностью заблокированных уже
  заворачивал OS-пробы на панель существующими правилами — firewall НЕ
  трогали.
- Компоненты: модель `BonusRequest` + миграция №3; `services/bonus.py`
  (антиспам 1/10мин, TTL 24ч); страница `/me` (полосы квот, кнопка
  «Попросить ещё времени», автообновление 20с); та же кнопка на `/blocked`;
  событие `bonus_request` → бот шлёт родителю `[+15][+30][до конца дня]
  [Отказать]` → `add_bonus_time`+reconcile, заявка помечается (идемпотентно).
- DNS-имя для закладки: `vremya.dom → 192.168.88.2` (AdGuard rewrite,
  выбрано владельцем). Закладка детям: `http://vremya.dom:8000/me`.
- Round-trip проверен на проде вживую (req#1: заявка → пуш → «Отказать» →
  denied за 346 мс, бонус не создан).
- **Эмпирика iOS (проверено на iPhone владельца, 2026-07-16)**: перехват
  работает — ввод любого `http://` адреса показывает заглушку мгновенно; но
  системное окно captive-portal при переподключении к ЗНАКОМОЙ сети iOS НЕ
  показывает (просто значок «нет интернета» в настройках Wi-Fi) — всплывает
  надёжно только для новых/забытых сетей. Вывод: главный вход для детей —
  иконка «Время» на домашнем экране; captive-portal — бонус для гостей.
  Это потолок технологии (Family Link решает агентом в ОС), не дефект.

```text
Оптика ── Huawei HG8546M (роутер, 192.168.0.1, режим БЕЗ моста — осознанно)
              │ LAN
          MikroTik hAP ac2 (ether1 ← DHCP 192.168.0.161; LAN 192.168.88.1/24)
              │  RouterOS 7.23.2 + firmware 7.23.2, admin известен владельцу
              ├─ Raspberry Pi 3B  192.168.88.2 (Debian 12, ssh: znz)
              │    ├─ AdGuard Home  :53 DNS, :3000 веб (admin)
              │    └─ панель HomeSec :8000 (systemd homesec) + pull-деплой
              ├─ Archer C6 (режим AP — семейный Wi-Fi 2.4/5)
              └─ Wi-Fi MikroTik: wlan2 5ГГц "Cherkash-5G" (осн., канал 36)
                                 wlan1 2.4ГГц "Podliva" (ГОСТЕВАЯ, в bridge-guest)
```

- Принудительный DNS включён: mark-connection `hs-dns` + dst-nat на Pi +
  **hairpin masquerade** (см. «Выстраданные правила» №3).
- Гостевая сеть: 192.168.90.0/24, изоляция от LAN, лимит 20М (queue hs-guest-limit).
- Панель управляет ТОЛЬКО address-list'ами `hs-*` и очередями `hs-dev-*`.
  Firewall/NAT/mangle-правила она не создаёт и не удаляет — их ставит .rsc.

## Секреты — В РЕПОЗИТОРИИ ИХ НЕТ

Все пароли лежат в `/opt/homesec/backend/.env` на малинке (chmod 600):
`ssh znz@192.168.88.2` (пароль знает владелец) → `cat /opt/homesec/backend/.env`.
Там же: пароль панели (HS_ADMIN_PASSWORD), AdGuard (HS_ADGUARD_*), API-пользователь
MikroTik `homesec` (HS_MIKROTIK_PASSWORD). Доступ MikroTik: `ssh admin@192.168.88.1`
(пароль у владельца; для старого host-key нужен `-o HostKeyAlgorithms=+ssh-rsa`).
Никогда не коммить пароли, `.env`, `mikrotik/homesec-configured.rsc` (в .gitignore).

## Рабочий процесс

- Проверка перед пушем — ВСЁ, что гоняет CI: `cd backend && pytest && ruff check app tests && mypy app`
  (129 тестов; интеграции замоканы недоступными портами). Ловили вживую: CI был
  красным из-за mypy, который локально не гоняли.
- CI: GitHub Actions на каждый push (`.github/workflows/ci.yml`): pytest + ruff + mypy.
  **CD НЕ ждёт зелёного CI** — малинка тянет main безусловно; сцепка «деплой только
  зелёных» — возможная доработка update.sh (владельцу предложено, не решено).
- **CD: push в `main` = деплой в прод.** Малинка раз в минуту тянет `main`
  (systemd `homesec-update.timer` → `deploy/update.sh`, read-only deploy key)
  и перезапускает панель. Рискованные изменения — через ветку, в main только зелёное.
  «Выкачено» определяется меткой `/var/lib/homesec/deployed_sha` (пишется ПОСЛЕ
  успешных pip и рестартов), а не совпадением HEAD: упавший деплой ретраится сам
  каждую минуту. Изменённые юниты из `deploy/*.service` тоже доезжают деплоем.
- Работать с сетью можно только изнутри неё (панель за NAT, извне не доступна).

## Выстраданные правила (каждое — реальный инцидент)

1. **Не включать принудительный DNS, пока Pi не отвечает на 192.168.88.2.**
   Иначе весь DNS дома уходит в никуда → «нет интернета». Поэтому импорт
   `homesec-base.rsc` создаёт заворот ВЫКЛЮЧЕННЫМ, а включает его отдельный
   `enable-adguard.rsc` с проверкой пинга малинки.
2. **IP самой малинки никогда не попадает в hs-managed/hs-blocked.**
   Она инфраструктура: её upstream-запросы (AdGuard→1.1.1.1) нельзя резать.
   Защита в коде: `get_self_ips()` в `backend/app/services/enforcement.py` — не удалять.
3. **ЛЮБОЙ dst-nat-заворот в той же подсети требует hairpin masquerade.**
   Простой dst-nat ломается: клиент слал на 8.8.8.8, ответ приходит с 88.2 →
   «reply from unexpected source», отвергается (симптом: часть сайтов не
   открывается). Единственная рабочая схема — mark-connection в mangle →
   dst-nat → srcnat masquerade по метке (DNS: метка `hs-dns`; HTTP block-page:
   `hs-blockpage`). `connection-nat-state=dstnat` в srcnat НЕ принимается
   RouterOS 7.23 — не пытаться снова. Раз masquerade прячет IP клиента,
   панель отвечает на перехват редиректом на HS_PANEL_LAN_URL — прямое
   соединение приходит с настоящим IP.
4. **Блокировка DoH — только tcp/udp 443 к списку hs-doh.** В hs-doh входят
   1.1.1.1/8.8.8.8; блокировать к ним ВЕСЬ трафик = зарезать upstream AdGuard.
5. **`.rsc`-скрипты одноразовые** (не идемпотентны): повторный импорт создаёт
   дубли правил. Проверять `print where comment~"hs:"` перед импортом.
6. **Легаси-драйвер wireless (7.x) врёт в свойстве `running`** (false при
   вещающем радио). Истина — в `/interface wireless monitor <iface> once`.
7. **Firmware (RouterBOOT) должен совпадать с версией RouterOS.** Рассинхрон
   (7.11 vs 7.23.2) давал обрыв WPA2-рукопожатия («unicast key exchange
   timeout»). Лечение: `/system routerboard upgrade` + reboot.
8. **5ГГц не поднимается — проверь DFS.** `frequency=auto` может выбрать
   радарный канал и застрять в radar-detecting. Ставить non-DFS: 5180 (36).

## Карта кода

| Путь | Что это |
|---|---|
| `backend/app/services/enforcement.py` | ЯДРО: расчёт желаемого состояния + reconcile (раз в минуту и после каждого изменения) |
| `backend/app/services/mikrotik.py` | RouterOS API (librouteros); только address-lists, queues, leases, connections |
| `backend/app/services/adguard.py` | AdGuard REST API: per-client блокировки сервисов, safe search |
| `backend/app/ai/tools.py` | Реестр инструментов — ЕДИНСТВЕННАЯ точка, через которую ИИ и бот трогают систему; guardrails зашиты в код (self-IP, только hs-*, аудит) |
| `backend/app/ai/` | ИИ-слой: client.py (Claude API + дневной бюджет), orchestrator.py (NL→tools, мутации только через кнопку), analyst.py (дайджест), watchdog.py (аномалии: эвристики + LLM-оформление) |
| `backend/app/bot/` | Telegram-бот, отдельный systemd-юнит homesec-bot: уведомления, health-алерты, команды, ИИ-канал |
| `backend/app/migrations.py` | Мини-миграции схемы (PRAGMA user_version), только аддитивные |
| `backend/app/routers/`, `templates/` | Веб-панель (FastAPI + Jinja2, без JS-фреймворков) |
| `mikrotik/homesec-base.rsc` | Базовый импорт (контроль создаётся выключенным) |
| `mikrotik/enable-adguard.rsc` | Отдельное включение принудительного DNS (с проверкой Pi) |
| `mikrotik/enable-block-page.rsc` | Опция: HTTP заблокированных -> страница «время вышло» (правило для hs-unknown создаётся выключенным!) |
| `mikrotik/emergency-dns-rollback.rsc` | Аварийный откат DNS одной командой (единственный .rsc, который можно импортировать повторно) |
| `mikrotik/pi-guard.rsc` | Сторож малинки: пинг раз в минуту, при смерти Pi роутер берёт DNS на себя + алерт в Telegram |
| `backend/app/services/device_links.py` | Связи устройства без FK: чистка при удалении, перенос при объединении |
| `backend/app/services/safetext.py` | Очистка недоверенного текста (DHCP-hostname, «причина» ребёнка) от якорей разбора |
| `deploy/` | systemd-юниты + install.sh + pull-деплой update.sh + pi-hardening.sh (watchdog/журналы/zram/retention) |
| `docs/05-operations.md` | Снапшот прода, health-чеки, откаты — читать перед работой с живой сетью |
| `docs/06-ai-multiagent-tz.md` | ТЗ ИИ-слоя: архитектура, guardrails, этапы (реализованы этапы 0–2) |

## Дорожная карта (согласована с владельцем)

Фаза 1 — **СДЕЛАНО**: Telegram-бот (уведомления о новых устройствах с
кнопками, /status /block /pause /resume /digest, health-алерты). Для запуска
на проде нужно заполнить HS_TELEGRAM_* в .env на малинке.
Фаза 3 (ИИ) — **СДЕЛАНО в базовой версии**: NL-управление через Claude API
(app/ai/orchestrator.py; Opus для рассуждений, Haiku для рутины), детектор
аномалий (ночная активность, всплеск DoH), ИИ-дайджест дня. Правила: любая
мутация от ИИ — только через кнопку подтверждения в Telegram; дневной бюджет
токенов HS_AI_DAILY_TOKEN_BUDGET; без ключа HS_ANTHROPIC_API_KEY ИИ молчит,
остальное работает.
Фаза 2 — **СДЕЛАНО** (ТЗ: docs/07-phase2-tz.md): квоты времени по категориям
(учёт по DNS-активности, бонусы через /bonus), страница «время вышло»
(/blocked, требует разовый импорт enable-block-page.rsc на роутере), портал
регистрации (/register), анти-MAC-рандомизация (метка случайного MAC,
кнопка «объединить» по совпадению hostname, несколько MAC на устройство
через device_macs; первая настоящая миграция схемы — №1 в migrations.py).

## Владелец

Общение — на русском. Малинка перепрофилирована под HomeSec с его согласия
(старые сервисы gradesentinel_bot/motionEye/n8n остановлены, docker-данные
сохранены на диске). Huawei в мост переводить пока НЕ хочет. Постоянные
SSH-ключи агента на устройства не ставить — только парольный доступ по сессии.
