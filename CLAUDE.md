# CLAUDE.md — контекст для ИИ-агентов

HomeSec — работающая система контроля домашней сети. Это НЕ учебный проект:
изменения в `main` автоматически деплоятся в реальную домашнюю сеть семьи.
Ошибка в правилах DNS/firewall оставляет дом без интернета.

## Состояние: РАЗВЁРНУТО И РАБОТАЕТ (срез 2026-07-29)

Датированные детали, разборы и история вынесены в
**`docs/10-status-2026-07.md`** (ЧП 27 июля и его вскрытие, что именно
применено на железе, правки 15 июля, устройство киллер-фичи «попросить ещё
времени»). Здесь — только то, что нужно держать в голове всегда.

- Панель, бот и ИИ работают на проде; первая волна исправлений по аудиту
  (PR #3) в `main` и на железе. pytest 130 / ruff / mypy зелёные.
- **Полный аудит кода и прода — `docs/09-audit-2026-07.md`** (102 находки:
  1 critical, 16 high, 38 medium, 47 low). Начинать с блока
  «Рекомендованный порядок работ» в шапке того файла.
- Не закрыто из аудита (следующие кандидаты): квота на человека, а не на
  устройство (2 гаджета = 2× лимит); карантин для новых устройств со
  случайным MAC; роли/второй родитель; IP как идентификатор на детских
  страницах (лечится на роутере: `add-arp=yes` + `arp=reply-only`);
  идемпотентность кнопок `pick:`/`nd:` в боте.
- Продуктовый аудит бота — `docs/08-bot-product-audit.md`: спринт 1 в проде
  (PR #2), спринты 2–3 ждут согласования владельца.
- Открыто у владельца: merge iPhone-дублей кнопкой бота; решение по
  `HS_AI_DAILY_TOKEN_BUDGET` (300k сгорают за вечер отчётов); сцепка «CD
  деплоит только зелёный CI» (предложено, не решено); спринты 2–3 аудита бота.
- Главный вход детей — закладка `http://vremya.dom:8000/me` (AdGuard rewrite
  `vremya.dom → 192.168.88.2`). **Не предлагать captive-portal как основной
  канал**: проверено на iPhone — при переподключении к ЗНАКОМОЙ сети iOS
  системного окна не показывает. Это потолок технологии (Family Link решает
  агентом в ОС), не дефект.

### Проверено на живом проде 2026-08-15 (ssh, только чтение)

Малинка перезагружалась 14 августа 09:17 — меры из `pi-hardening.sh` реально
действуют, не «фиктивны»:

- **Лимиты памяти работают.** Панель 97 МБ (High 180 / Max 300), бот 217 МБ
  (High 220 / Max 350) — бот подобрался к мягкому потолку вплотную, но
  `memory.events` у обоих юнитов нули (ни разу не throttled, ни одного OOM).
  Замер июля (бот 141 МБ) устарел — следить.
- **Проверять контроллер памяти НЕ через `/proc/cgroups`.** Здесь cgroup **v2**,
  а в `/proc/cgroups` перечислены только v1-контроллеры — `grep ^memory` там
  пусто ДАЖЕ когда всё работает (ровно этот ложный минус и держал пункт
  открытым). Правильно: `cat /sys/fs/cgroup/cgroup.controllers` (должен быть
  `memory`) и `systemctl show homesec -p MemoryCurrent` (не «[not set]»).
  В `/proc/cmdline` при этом видно и `cgroup_disable=memory` от прошивки, и
  наши `cgroup_enable=memory cgroup_memory=1` из `cmdline.txt` — побеждают наши.
- Watchdog `RuntimeWatchdogUSec=15s`, `/dev/watchdog` есть; zram активен
  (`/dev/zram0` 453 МБ, приоритет 100) + файл подкачки 512 МБ; журнал ограничен
  дроп-ином `journald.conf.d/10-homesec.conf` (64 МБ; в самом `journald.conf`
  лежат более старые 50 МБ — выигрывает дроп-ин), занято 62 МБ.
- Панель, бот, AdGuard, оба таймера — active, `NRestarts=0` у всех троих с
  момента загрузки; `homesec-dnscheck` тикает каждую минуту и завершается
  успешно. Выкачено `39680b9` — совпадает с main.
- Диск: 19 ГБ из 28 (72%), свободно 7,6 ГБ.

### Испытание сторожа 2026-08-15: обнаружение ✅, спасение ✗ (НЕ ЧИНИТЬ ВСЛЕПУЮ)

Провели ровно ту аварию (`systemctl stop AdGuardHome` 17:43:40 → `start` 17:49:20),
наблюдая с ноутбука в LAN (192.168.88.251, резолвер по DHCP = 88.2):

- ✅ **Обнаружение и алерт работают.** Через ~3 минуты роутер переключился,
  владелец получил алерт в Telegram.
- ✅ **Возврат контроля автоматический.** После старта AdGuard заворот вернулся:
  проба `dig @1.1.1.1 ch txt id.server` во время аварии ОТВЕЧАЛА (значит заворот
  снят), а после восстановления снова молчит (значит запросы опять уходят в
  AdGuard). Это хороший способ проверить заворот с клиента, не заходя на роутер.
- ✗ **«Спасательный круг» дом не спас.** Все 5 минут `dig @192.168.88.2`
  (путь, которым ходит КАЖДОЕ устройство дома — DHCP раздаёт 88.2) молчал:
  таймаут, системный резолв не работал. Отвечал только `dig @192.168.88.1`
  (роутер напрямую) и внешние DNS — то есть спаслись бы лишь те, у кого DNS
  прописан руками. Таких в доме нет. Заголовок скрипта («интернет дома жив,
  сайты открываются») в этой части НЕ соответствует поведению.

**Корень (роутер, тот же вечер).** Журнал сторожа: перехват 17:45:57, возврат
17:49:51 — сам сторож отработал. Но все пять правил «круга» показали **0 пакетов**,
а `/interface bridge settings` — `use-ip-firewall=no` при активном bridge fast-path.
То есть запросы к 88.2 идут внутри моста на L2 и в IP-firewall не попадают в
принципе: правила исправны, ошибочна предпосылка. Это выстраданное правило №9.

**Что сделано (2026-08-15, на роутере и в .rsc).** DHCP теперь раздаёт ВТОРОЙ
резолвер — LAN: `192.168.88.2, 192.168.88.1`; гостям: `192.168.88.2, 192.168.90.1`.
Уйти с мёртвого адреса способен только сам клиент, и делает он это, лишь зная
запасной. Аренда в LAN 10 минут, так что парк подхватывает список сам.
Пока малинка жива, запросы к 88.1 всё равно заворачиваются на AdGuard (контроль
не слабеет); в аварии сторож заворот снимает, и роутер отвечает сам. Цена:
запрос, ушедший на запасной резолвер при случайном таймауте основного, приходит
в AdGuard от имени роутера (правило №3) — для него персональная политика и учёт
квоты не применятся. Осознанный размен: полностью слепой дом хуже.

**Повторное испытание 2026-08-15 18:12 — почин подтверждён вживую.** Та же
авария, наблюдение с ноутбука обычным резолвом (`dig` по списку из DHCP):

| Время | Что происходило |
|---|---|
| 18:12:54 | AdGuard остановлен |
| 18:13:15–18:15:39 | резолва нет — заворот ещё включён, и запрос на запасной 88.1 он тоже отправляет мёртвой малинке |
| **18:15:57** | сторож переключился — **в ту же секунду резолв пошёл через 192.168.88.1** и дальше не прерывался |
| 18:20:54 | AdGuard поднят обратно |
| 18:21:47 | роутер сам вернул контроль: заворот включён, «круг» выключен, `/ip dns servers=192.168.88.2` |

Итог: слепое окно ~3 минуты (раньше — до вмешательства человека, в ЧП это был
час), дальше дом живёт сам. Окно = время до переключения сторожа; его можно
ужать до ~2 минут, снизив `failsToSwitch` с 3 до 2 в `pi-guard.rsc` (не делали).

### Что осталось (требует человека)

1. Место на карте: ~5 ГБ в /home от старых задач (`.vscode-server` 2,2 ГБ,
   `it-projects` 1,5 ГБ, `.npm` 489 МБ). На роутере свободно 900 КБ из 16 МБ
   (два .rsc заняли ~17 КБ).

## Топология и что чем управляет

```text
Оптика ── Huawei HG8546M (роутер, 192.168.0.1, режим БЕЗ моста — осознанно)
              │ LAN
          MikroTik hAP ac2 (ether1 ← DHCP 192.168.0.161; LAN 192.168.88.1/24)
              │  RouterOS 7.23.2 + firmware 7.23.2, admin известен владельцу
              ├─ Raspberry Pi 3B  192.168.88.2 (Debian 12, ssh: znz)
              │    ├─ AdGuard Home  :53 DNS, :3000 веб (admin)
              │    └─ панель HomeSec :8000 (systemd homesec) + pull-деплой
              ├─ Archer C6 (режим AP — семейный Wi-Fi 2.4/5)
              └─ Wi-Fi MikroTik: wlan2 5ГГц "Cherkash-5G" (осн., канал 36)
                                 wlan1 2.4ГГц "Podliva" (ГОСТЕВАЯ, в bridge-guest)
```

- Принудительный DNS включён: mark-connection `hs-dns` + dst-nat на Pi +
  **hairpin masquerade** (см. «Выстраданные правила» №3).
- Гостевая сеть: 192.168.90.0/24, изоляция от LAN, лимит 20М (queue hs-guest-limit).
- Панель управляет ТОЛЬКО address-list'ами `hs-*` и очередями `hs-dev-*`.
  Firewall/NAT/mangle-правила она не создаёт и не удаляет — их ставит .rsc.

## Секреты — В РЕПОЗИТОРИИ ИХ НЕТ

Все пароли лежат в `/opt/homesec/backend/.env` на малинке (chmod 600):
`ssh znz@192.168.88.2` (пароль знает владелец) → `cat /opt/homesec/backend/.env`.
Там же: пароль панели (HS_ADMIN_PASSWORD), AdGuard (HS_ADGUARD_*), API-пользователь
MikroTik `homesec` (HS_MIKROTIK_PASSWORD). Доступ MikroTik: `ssh admin@192.168.88.1`
(пароль у владельца; для старого host-key нужен `-o HostKeyAlgorithms=+ssh-rsa`).
Никогда не коммить пароли, `.env`, `mikrotik/homesec-configured.rsc` (в .gitignore).
Смотреть — только по ssh: скопированный на Mac `backend/.env` направит локальную
панель на БОЕВОЙ роутер (см. «Локальная разработка»).

## Рабочий процесс

### Локальная разработка (Mac; всё в этом разделе проверено 2026-08-15)

Окружения в репозитории нет, и pytest/ruff/mypy НЕ в `requirements.txt` (осознанно:
чтобы не тащить их на малинку) — ставить отдельно. Нужен Python 3.11 (на нём CI и
прод); на Mac его может не быть вовсе — `uv` скачает сам:

```bash
uv venv --python 3.11 .venv     # из корня репозитория (.venv в .gitignore)
source .venv/bin/activate       # ОБЯЗАТЕЛЬНО: иначе pytest/ruff/mypy не на PATH
cd backend && uv pip install -r requirements.txt pytest ruff mypy
```

Все команды ниже — при активированном venv и из `backend/`.

- Проверка перед пушем — ВСЁ, что гоняет CI: `pytest && ruff check . && mypy app`
  (130 тестов, ~3 с; интеграции замоканы недоступными портами). Ловили вживую:
  CI был красным из-за mypy, который локально не гоняли.
- Один файл / один тест: `pytest tests/test_quota.py`,
  `pytest tests/test_quota.py -k bonus`.
- **`backend/.env` на машине разработки быть не должно.** `tests/conftest.py`
  перекрывает только семь HS_*-переменных, остальные молча подтягиваются из
  `.env` (`env_file` в `app/config.py`). Проверено: одна строка
  `HS_ANTHROPIC_API_KEY=…` в `backend/.env` роняет 3 теста в `test_ai.py`
  (они проверяют поведение БЕЗ ключа) — а выглядит как «мои правки сломали тесты».
- Панель локально:
  `HS_SCHEDULER_ENABLED=false HS_DATABASE_PATH=/tmp/dev.db uvicorn app.main:app --reload`
  → `http://127.0.0.1:8000`, вход `admin`/`admin` (дефолты `config.py`, пока нет `.env`).
  `HS_SCHEDULER_ENABLED=false` обязателен: иначе минутный reconcile сам полезет в
  роутер. Изоляция от прода держится ровно на отсутствии `backend/.env` — если
  скопировать туда боевой `.env` с малинки (раздел «Секреты» посылает его читать),
  «локальная» панель возьмёт настоящие HS_MIKROTIK_*/HS_ADGUARD_* и из домашней
  сети начнёт править address-list'ы на живом роутере. Смотреть секреты по ssh,
  файл в репозиторий не копировать.

### CI и деплой в прод

- CI: GitHub Actions на каждый push (`.github/workflows/ci.yml`): pytest + ruff + mypy.
  **CD НЕ ждёт зелёного CI** — малинка тянет main безусловно; сцепка «деплой только
  зелёных» — возможная доработка update.sh (владельцу предложено, не решено).
- **CD: push в `main` = деплой в прод.** Малинка раз в минуту тянет `main`
  (systemd `homesec-update.timer` → `deploy/update.sh`, read-only deploy key)
  и перезапускает панель. Рискованные изменения — через ветку, в main только зелёное.
  «Выкачено» определяется меткой `/var/lib/homesec/deployed_sha` (пишется ПОСЛЕ
  успешных pip и рестартов), а не совпадением HEAD: упавший деплой ретраится сам
  каждую минуту. Изменённые юниты из `deploy/*.service` тоже доезжают деплоем.
- Работать с сетью можно только изнутри неё (панель за NAT, извне не доступна).

## Выстраданные правила (каждое — реальный инцидент)

1. **Не включать принудительный DNS, пока Pi не отвечает на 192.168.88.2.**
   Иначе весь DNS дома уходит в никуда → «нет интернета». Поэтому импорт
   `homesec-base.rsc` создаёт заворот ВЫКЛЮЧЕННЫМ, а включает его отдельный
   `enable-adguard.rsc` с проверкой пинга малинки.
2. **IP самой малинки никогда не попадает в hs-managed/hs-blocked.**
   Она инфраструктура: её upstream-запросы (AdGuard→1.1.1.1) нельзя резать.
   Защита в коде: `get_self_ips()` в `backend/app/services/enforcement.py` — не удалять.
3. **ЛЮБОЙ dst-nat-заворот в той же подсети требует hairpin masquerade.**
   Простой dst-nat ломается: клиент слал на 8.8.8.8, ответ приходит с 88.2 →
   «reply from unexpected source», отвергается (симптом: часть сайтов не
   открывается). Единственная рабочая схема — mark-connection в mangle →
   dst-nat → srcnat masquerade по метке (DNS: метка `hs-dns`; HTTP block-page:
   `hs-blockpage`). `connection-nat-state=dstnat` в srcnat НЕ принимается
   RouterOS 7.23 — не пытаться снова. Раз masquerade прячет IP клиента,
   панель отвечает на перехват редиректом на HS_PANEL_LAN_URL — прямое
   соединение приходит с настоящим IP.
4. **Блокировка DoH — только tcp/udp 443 к списку hs-doh.** В hs-doh входят
   1.1.1.1/8.8.8.8; блокировать к ним ВЕСЬ трафик = зарезать upstream AdGuard.
5. **`.rsc`-скрипты одноразовые** (не идемпотентны): повторный импорт создаёт
   дубли правил. Проверять `print where comment~"hs:"` перед импортом.
6. **Легаси-драйвер wireless (7.x) врёт в свойстве `running`** (false при
   вещающем радио). Истина — в `/interface wireless monitor <iface> once`.
7. **Firmware (RouterBOOT) должен совпадать с версией RouterOS.** Рассинхрон
   (7.11 vs 7.23.2) давал обрыв WPA2-рукопожатия («unicast key exchange
   timeout»). Лечение: `/system routerboard upgrade` + reboot.
8. **5ГГц не поднимается — проверь DFS.** `frequency=auto` может выбрать
   радарный канал и застрять в radar-detecting. Ставить non-DFS: 5180 (36).
9. **Роутер НЕ ВИДИТ трафик между соседями по своему же мосту.** Запрос с
   ноутбука на 192.168.88.2 коммутируется на L2 (`use-ip-firewall=no`,
   bridge fast-path) и в IP-firewall не попадает вовсе — ни mangle, ни dst-nat
   его не поймают. Поэтому «спасательный круг» сторожа (заворот запросов к
   мёртвой малинке на роутер) не сработал в испытании 2026-08-15: 0 пакетов на
   всех пяти правилах при живом переключении. Для ГОСТЕЙ те же правила работают —
   у них другая подсеть, трафик реально маршрутизируется. Лечится не правилами
   firewall, а тем, что клиент сам знает запасной резолвер (второй DNS в DHCP);
   `use-ip-firewall=yes` тоже лечит, но ценой выключенного fast-path на весь
   локальный трафик — отвергнуто.

## Карта кода

| Путь | Что это |
|---|---|
| `backend/app/services/enforcement.py` | ЯДРО: расчёт желаемого состояния + reconcile (раз в минуту и после каждого изменения) |
| `backend/app/scheduler.py` | Три отдельные минутные/суточные задачи: reconcile, учёт квот (нарочно врозь — reconcile дёргается и вне расписания, счёт бы задвоился), ежедневная уборка |
| `backend/app/services/quota.py` | Квоты времени: учёт активных минут по журналу AdGuard, бонусы, `exhausted()` для ядра |
| `backend/app/services/restrictions.py` | Только чтение: «почему сейчас нет интернета» для `/blocked` и `/me` |
| `backend/app/services/mikrotik.py` | RouterOS API (librouteros); только address-lists, queues, leases, connections |
| `backend/app/services/adguard.py` | AdGuard REST API: per-client блокировки сервисов, safe search |
| `backend/app/ai/tools.py` | Реестр инструментов — ЕДИНСТВЕННАЯ точка, через которую ИИ и бот трогают систему; guardrails зашиты в код (self-IP, только hs-*, аудит) |
| `backend/app/ai/` | ИИ-слой: client.py (Claude API + дневной бюджет), orchestrator.py (NL→tools, мутации только через кнопку), analyst.py (дайджест), watchdog.py (аномалии: эвристики + LLM-оформление) |
| `backend/app/bot/` | Telegram-бот, отдельный systemd-юнит homesec-bot: уведомления, health-алерты, команды, ИИ-канал |
| `backend/app/migrations.py` | Мини-миграции схемы (PRAGMA user_version), только аддитивные |
| `backend/app/routers/`, `templates/` | Веб-панель (FastAPI + Jinja2, без JS-фреймворков) |
| `backend/app/routers/portal.py` | ПУБЛИЧНЫЕ страницы (без логина, по IP клиента): `/me` (детская, «попросить ещё времени»), `/blocked`, `/register` |
| `backend/app/config.py` | Все `HS_*`-настройки с дефолтами в одном месте; читает `backend/.env` |
| `mikrotik/homesec-base.rsc` | Базовый импорт (контроль создаётся выключенным) |
| `mikrotik/enable-adguard.rsc` | Отдельное включение принудительного DNS (с проверкой Pi) |
| `mikrotik/enable-block-page.rsc` | Опция: HTTP заблокированных -> страница «время вышло» (правило для hs-unknown создаётся выключенным!) |
| `mikrotik/emergency-dns-rollback.rsc` | Аварийный откат DNS одной командой (единственный .rsc, который можно импортировать повторно) |
| `mikrotik/pi-guard.rsc` | Сторож малинки: раз в минуту РЕЗОЛВИТ имя через Pi (`:resolve … server=`, не пинг — ЧП 27.07 пинг бы не поймал); 3 неудачи → алерт в Telegram + «спасательный круг». Обнаружение и возврат контроля проверены вживую 2026-08-15, **сам заворот на роутер НЕ работает** — см. «Испытание сторожа» |
| `backend/app/services/device_links.py` | Связи устройства без FK: чистка при удалении, перенос при объединении |
| `backend/app/services/safetext.py` | Очистка недоверенного текста (DHCP-hostname, «причина» ребёнка) от якорей разбора |
| `deploy/dns-selfheal.sh` | Сторож DNS на малинке (таймер homesec-dnscheck): перезапускает AdGuard, если он перестал отвечать |
| `deploy/` | systemd-юниты + install.sh + pull-деплой update.sh + pi-hardening.sh (watchdog/журналы/zram/retention) |
| `docs/05-operations.md` | Снапшот прода, health-чеки, откаты — читать перед работой с живой сетью |
| `docs/10-status-2026-07.md` | История и датированные срезы июля 2026: разбор ЧП 27.07, что применено на железе, правки 15.07, устройство киллер-фичи |
| `docs/06-ai-multiagent-tz.md` | ТЗ ИИ-слоя: архитектура, guardrails, этапы (реализованы этапы 0–2) |

## Дорожная карта (согласована с владельцем)

Фаза 1 — **СДЕЛАНО**: Telegram-бот (уведомления о новых устройствах с
кнопками, /status /block /pause /resume /digest, health-алерты). Для запуска
на проде нужно заполнить HS_TELEGRAM_* в .env на малинке.
Фаза 3 (ИИ) — **СДЕЛАНО в базовой версии**: NL-управление через Claude API
(app/ai/orchestrator.py; Opus для рассуждений, Haiku для рутины), детектор
аномалий (ночная активность, всплеск DoH), ИИ-дайджест дня. Правила: любая
мутация от ИИ — только через кнопку подтверждения в Telegram; дневной бюджет
токенов HS_AI_DAILY_TOKEN_BUDGET; без ключа HS_ANTHROPIC_API_KEY ИИ молчит,
остальное работает.
Фаза 2 — **СДЕЛАНО** (ТЗ: docs/07-phase2-tz.md): квоты времени по категориям
(учёт по DNS-активности, бонусы через /bonus), страница «время вышло»
(/blocked, требует разовый импорт enable-block-page.rsc на роутере), портал
регистрации (/register), анти-MAC-рандомизация (метка случайного MAC,
кнопка «объединить» по совпадению hostname, несколько MAC на устройство
через device_macs; первая настоящая миграция схемы — №1 в migrations.py).

## Владелец

Общение — на русском. Малинка перепрофилирована под HomeSec с его согласия
(старые сервисы gradesentinel_bot/motionEye/n8n остановлены, docker-данные
сохранены на диске). Huawei в мост переводить пока НЕ хочет. Постоянные
SSH-ключи агента на устройства не ставить — только парольный доступ по сессии.
